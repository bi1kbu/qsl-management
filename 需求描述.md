1、我希望改为使用halo2的常规方案，java的
2、数据先不管，留一个导出（备份）导入（去重更新备份）功能就行
3、先按照本地开发来搞吧，本地开发完确认没问题了再上线
4、插件前后台都有，前台提供游客查询某个呼号的卡片是否发出、是否被签收（但是看不到地址，只能看到是否发出以及发出时间、是否收到卡片及收到时间），前台还面向注册用户提供换卡申请（网络eyeball卡交换申请）。其他功能则为后台功能。
5、前台：通过呼号查询卡片情况（该呼号发出的卡片，本站所有者是否收到；本站所有者是否向该呼号发出了卡片）
后台：本站配置（本台呼号、姓名、电话、邮编、收件地址、其他备注描述、本台设备（列表）、本台天线（列表）、本台功率（列表）等）
其他配置：卡片局（卡片局名称、邮编、地址、电话）、模型配置（用于后续自动识别和获取地址）
总览（后续涉及条目的综合只读显示，可配置显示字段，其余字段点击条目后展开显示）
通联录入（日期、时间（默认自动填充但可手动修改）、时区（UTC/UTC+8）、频率、模式、电台位置、本台设备（下拉）、本台天线（下拉）、对方呼号、本台给对方的信号报告（CW模式默认599，其他模式默认59）、对方给本台的信号报告（CW模式默认599，其他模式默认59）、本台功率（下拉）、对方功率（填写）、备注）
卡片录入（卡片类型（通联卡（关联通联记录）/收听卡/眼球卡）、日期、时间（通联卡使用通联记录的日期时间，其他卡片默认自动填充但可手动修改）、时区（UTC/UTC+8）、对方呼号、姓名、电话、邮编、地址、是否收到卡片、是否发出卡片；其中姓名电话邮编地址可根据QRZ.cn和QRZ.com尝试自动获取并使用大模型解析为发件地址）
卡片打印（呼号选择（可多选）、导出（条目对应的呼号、日期、时间、时区、卡片类型（通联卡/收听卡/眼球卡）、频率、模式、电台位置、本台设备、本台天线、对方呼号、本台给对方的信号报告、对方给本台的信号报告、本台功率、对方功率、是否已经收到卡片、备注）、是否标记已打印）
信封打印（呼号选择（可多选）、导出（条目对应的呼号、姓名、电话、邮编、收件地址；如果识别到）、是否标记已打印）
发信确认（通过呼号选择 卡片录入 的条目，确认后自动标记已打印卡片和已打印信封；记录发信确认的日期时间以供他人前台查询）
收信确认（通过呼号选择 卡片录入 的条目或创建新的待发眼球卡条目（同卡片录入或跳转到卡片录入），确认后自动标记卡片为已收到卡片）
6、外部依赖二期再做
7、允许修改（重构）excel导出模板
8、一次性完成旧系统全部功能且符合我提出的新要求