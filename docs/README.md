# QSL 项目文档索引

## 目录说明
1. `spec/`：产品定义、API 合同、结构化清单与字段汇总。
2. `plans/`：阶段计划、冻结方案与后续开发安排。
3. `reports/`：验证记录、场景走查与运行态验收汇总。
4. `templates/`：邮件、通知等模板参考。
5. `archive/`：历史或迁移前文档归档。

## 根目录约定
仓库根目录仅保留 `README.md` 与 `agents.md` 两个 Markdown 文件，其他项目文档统一归入本目录。
